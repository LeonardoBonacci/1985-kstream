spring.application.name: heroes-tip-purger-app

spring:
  kafka:
    bootstrap-servers: localhost:9092

    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.value.default.type: guru.bonacci.heroes.domain.Transfer  
        
  redis:
    database: 0
    host: localhost
    port: 6379
    password: mypass
    timeout: 10000
